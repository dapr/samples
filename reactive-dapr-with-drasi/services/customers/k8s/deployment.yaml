apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers
  labels:
    app: customers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customers
  template:
    metadata:
      labels:
        app: customers
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "customers"
        dapr.io/app-port: "8000"
        dapr.io/enable-api-logging: "true"
        dapr.io/log-level: "info"
    spec:
      containers:
      - name: customers
        image: customers:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: DAPR_STORE_NAME
          value: "customers-store"
        - name: LOG_LEVEL
          value: "INFO"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: customers
  labels:
    app: customers
spec:
  selector:
    app: customers
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: customers-stripprefix
spec:
  stripPrefix:
    prefixes:
    - /customers-service
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: customers
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: default-customers-stripprefix@kubernetescrd
spec:
  rules:
  - http:
      paths:
      - path: /customers-service
        pathType: Prefix
        backend:
          service:
            name: customers
            port:
              number: 80